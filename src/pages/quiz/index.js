import React from "react";
import Tab from "../../components/tabs";

const Quiz = () => {
  return (
    <div className="quiz-page d-flex" style={{paddingTop:25}}>
        <Tab />
    </div>
  );
};

export default Quiz;
